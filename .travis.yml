language: python

python:
  - "3.6"

services:
  - docker

script:
 - echo "skipping tests"
 
before_deploy:
    - docker build -t 3DiRK/z2m-nwm .
    
deploy:
  provider: script
  script: bash docker_push
  on:
    branch: master
